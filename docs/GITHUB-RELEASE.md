# GitHub release

Push the contents of the extracted `Skyrim-Forge-3.0.2` directory to the repository root. Wait for CI and CodeQL. Create tag `v3.0.2` only after all required checks pass.

The release workflow builds the deterministic repository ZIP, Python wheel, source distribution, checksums, validation report, and SBOM.
