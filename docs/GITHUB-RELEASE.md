# GitHub release

Push the contents of the extracted `Skyrim-Forge-4.2.5` directory to the repository root. Wait for CI and CodeQL. Create tag `v4.2.5` only after all required checks pass.

The release workflow builds the deterministic repository ZIP, Python wheel, source distribution, checksums, validation report, and SBOM.
